import { AddressFormatPipePipe } from './address-format-pipe-pipe';

describe('AddressFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AddressFormatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
